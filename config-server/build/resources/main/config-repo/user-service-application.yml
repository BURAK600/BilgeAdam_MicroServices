server:
  port: 9096

spring:

  zipkin:
    enabled: true
    base-url: http://localhost:9411
    service:
      name: user-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://34.173.108.167:1111/java3user
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


myredis:
  host: localhost
  port: 6379

myapplication:
  user-service:
    feign-client: http://localhost:9096/api/v1/userprofile
  elastic-service:
    feign-client: http://localhost:9099/api/v1/elastic


# JWT
myjwt:
  secretkey: ${secretKey}
  audience: ${audience}
  issuer: ${issuer}

