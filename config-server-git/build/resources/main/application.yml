server:
  port: 8889

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: ""
          default-label: main
          skip-ssl-validation: true
          username:
          password: